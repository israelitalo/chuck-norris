import styled from "styled-components";

export const Title = styled.div`
    text-align: center;
    h1 {
        margin-top: 1rem;
        font-weight: 400;
        display: flex;
        flex-direction: column;
        span {
            font-weight: 700;
            color: var(--primary);
        }
    }
`;